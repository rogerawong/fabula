Synthetic Hugo fixtures for docs/16 (reparenting). Authored for this
repo, not vendored: gapped weights (tasks), an all-unweighted
destination (guides), a leaf bundle with a resource (bundle/leafy), a
path-collision pair (tasks/alpha.md vs collide/alpha.md), a page that
already carries aliases:, and a slug that a move's alias would collide
with (tasks/delta.md claims alpha-taken).
