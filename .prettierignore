dist/
coverage/
docs/
README.md
pnpm-lock.yaml
LICENSE
*.yml.snap
src/formats/**/fixtures/
src/formats/**/samples/
src/collections/__tests__/fixtures/
# Generated by the impeccable design hook on every UI edit, and excluded
# from git locally rather than tracked. Without this line `format:check`
# fails on a file no commit contains — verified on a clean tree, so the
# failure predates any change of ours.
.impeccable/hook.cache.json
